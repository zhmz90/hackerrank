#include <math.h>
#include <stdlib.h>
#include <stdio.h>


int main(){

    printf("sizeof(char):%lu\n",sizeof(char));
    char* time = (char *)malloc(10240 * sizeof(char));
    scanf("%s", time);

    printf("%s",time);

    bool 
true
    return 0;
}
